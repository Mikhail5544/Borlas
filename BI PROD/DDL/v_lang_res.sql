CREATE OR REPLACE FORCE VIEW V_LANG_RES AS
SELECT
	   	  		  	   			  LANG_RES_ID,
								  NAME,
								  BRIEF,
								  OBJ_ID,
								  LANG_ID,
								  ATTR_ID,
								  ELEMENT_TYPE
FROM LANG_RES;

